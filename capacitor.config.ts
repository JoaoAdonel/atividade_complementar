import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'atividade_complementar',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
